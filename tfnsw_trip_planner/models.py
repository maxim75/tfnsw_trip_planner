"""Backward-compat shim — the models/ package supersedes this file.

Python's import system loads the models/ package (directory) instead of this
module when both exist.  This file is kept only for version-control history
and can be safely deleted.
"""
# This module is intentionally empty.
